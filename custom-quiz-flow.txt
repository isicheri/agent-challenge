---

## 🔄 User Flow Examples

### **Flow 1: Complete Study → Take Quiz**
```
1. User marks last subtopic complete
   → PATCH /api/subtopics/complete
   
2. Backend auto-generates quiz
   → Returns quiz object with quizGenerated: true
   
3. Frontend shows "Quiz Ready!" notification
   
4. User clicks "Start Quiz"
   → POST /api/quiz/[quizId]/start
   
5. User answers questions and submits
   → POST /api/quiz/[quizId]/submit
   
6. Frontend shows results with score/percentage
```

### **Flow 2: View Quiz History**
```
1. User goes to "My Quizzes" page
   → GET /api/users/[userId]/quiz-history
   
2. Frontend displays:
   - Completed quizzes with scores
   - In-progress quizzes with [Resume] button
   - Stats dashboard (avg score, pass rate, etc.)
   
3. User clicks on completed quiz
   → GET /api/quiz/attempts/[attemptId]
   → Shows detailed review (correct/wrong answers)
```

### **Flow 3: Resume Incomplete Quiz**
```
1. User sees incomplete quiz in history
   
2. User clicks "Resume Quiz"
   → GET /api/quiz/attempts/[attemptId]/resume
   
3. Frontend loads quiz with already-answered questions disabled
   
4. User completes remaining questions
   → POST /api/quiz/[quizId]/submit


   ✨ Key Features
✅ Auto Quiz Generation - Quizzes automatically created when study plan completed
✅ Score Tracking - Automatic scoring with percentage calculation
✅ Quiz History - View all past attempts (completed & incomplete)
✅ Resume Quizzes - Continue where you left off
✅ Detailed Results - Review correct/incorrect answers
✅ Statistics Dashboard - Average score, pass rate, best/worst scores
✅ Time Tracking - Track how long quizzes take